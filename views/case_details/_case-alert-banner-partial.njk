{%- from "moj/components/alert/macro.njk" import mojAlert -%}

{% set currentStatus = client.caseStatus %}

{% if currentStatus == "completed" %}
  {{ mojAlert({
      variant: "information",
      title: "Case completed",
      showTitleAsHeading: true,
      dismissible: false,
      text: client.providerClosedBanner
    }) }}
{% elif currentStatus == "closed" %}
  {% set outcomeDescription = t('common.outcomeCode.' + client.outcome_code) if client.outcome_code else '' %}
  {{ mojAlert({
      variant: "information",
      title: "Case closed",
      showTitleAsHeading: true,
      dismissible: false,
      html: outcomeDescription + "<p>" + client.providerClosedBanner + "</p>"
    }) }}
{% elif currentStatus == "pending" %}
  {{ mojAlert({
      variant: "warning",
      title: "Case pending",
      showTitleAsHeading: true,
      dismissible: false,
      html: client.state_note + "<p>" + client.providerViewedBanner + "</p>"
    }) }}
{% endif %}